---
apiVersion: v1
kind: Service
metadata:
  name: mariadb
  namespace: openknowledge
spec:
  selector:
    app: mariadb
  ports:
    - port: 3306
      targetPort: 3306
  clusterIP: None  # headless — StatefulSet usa DNS direto para os pods
