---
apiVersion: v1
kind: ConfigMap
metadata:
  name: wordpress-config
  namespace: openknowledge
data:
  WORDPRESS_DB_HOST: "mariadb:3306"
  WORDPRESS_DB_NAME: "wordpress"
  # WP_HOME e WP_SITEURL são sobrescritos no overlay de produção via
  # WORDPRESS_CONFIG_EXTRA no secret (valores sensíveis ao domínio).
