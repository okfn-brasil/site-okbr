---
# Armazena os uploads de mídia do WordPress (wp-content/uploads/).
# RWO é suficiente para 1 réplica. Para escalar horizontalmente,
# trocar por ceph-filesystem (RWX) ou usar um plugin de offload para S3.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wordpress-uploads
  namespace: openknowledge
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block-hdd
  resources:
    requests:
      storage: 10Gi
---
# Armazena os plugins do WordPress (wp-content/plugins/).
# Permite instalar/atualizar plugins via painel ou WP-CLI com persistência entre deploys.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wordpress-plugins
  namespace: openknowledge
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block-hdd
  resources:
    requests:
      storage: 10Gi
